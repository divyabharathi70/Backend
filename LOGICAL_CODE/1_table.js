for (let a=1;a<=5;a++){
    console.log(`${a}*8=${8*a}`);
}
for (let a=1;a<=10;a++){
    console.log(`${a} 's square is ${a*a}`);
}

// initially variable a is init as 1 
// the loop gonna get executed untill variable a is <= 5 and after each loop execution a get increamented.

// we use variable a to cal table 8

// a = 1 - 1 × 8 = 8
// a = 2 - 2 × 8 = 16
// a = 3 - 3 × 8 = 24
// a = 4 - 4 × 8 = 32
// a = 5 - 5 × 8 = 40

// likewise Square number

// a = 1 - 1 × 1 = 1
// a = 2 - 2 × 2 = 4
// a = 3 - 3 × 3 = 9
// a = 4 - 4 × 4 = 16
// a = 5 - 5 × 5 = 25